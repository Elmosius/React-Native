import { ScrollView, Text } from 'react-native';

export default function TestScreen() {
  return (
    <ScrollView>
      <Text className='font-bold text-xl text-red-500'>Test</Text>
    </ScrollView>
  );
}
